<StackLayout loaded="onLoaded">
    <GridLayout>
        <ListView id="listview" items="{{ navigationItems }}" itemTap="onNavigationItemTap">
            <ListView.itemTemplate>
                <Label text="{{ title }}"/>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</StackLayout>