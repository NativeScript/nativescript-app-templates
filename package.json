{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "publish.all": "lerna exec --no-bail --stream --concurrency 1 -- \"npm publish\""
  },
  "devDependencies": {
    "lerna": "~3.14.0"
  }
}
